import { PortRemovePipe } from './port-remove.pipe';

describe('PortRemovePipe', () => {
  it('create an instance', () => {
    const pipe = new PortRemovePipe();
    expect(pipe).toBeTruthy();
  });
});
